import 'package:flutter/material.dart';

class CategrayPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:Text("分类")
      ),
      body: Center(
        child: Center(
          child: Text("分类内容"),
        ),
      ),
    );
  }
}